# P66 Phase 1 — Variant Intelligence

Per-cover `variant_score`, `variant_tier` (S/A/B/C), and `variant_reason` from catalog variants, FOC-linked issues, ratio/foil/artist/character signals, and buy-queue demand (read-only).

Endpoints: `GET/POST /api/v1/variant-intelligence/latest|build`
