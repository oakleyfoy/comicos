# P66 Phase 3 — Market Pricing

Provider abstraction with `STUB` implementation today. Observations include FMV, trend, liquidity, and confidence per variant.

Future sources: eBay sold, GoCollect, CovrPrice, Heritage, ComicPriceGuide.

Endpoints: `GET/POST /api/v1/market-pricing/latest|build`
