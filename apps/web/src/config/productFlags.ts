/** B1: legacy customer_order / order_item write retirement (reads still work). */
export const LEGACY_CUSTOMER_ORDERS_WRITES_ENABLED = false;

export const LEGACY_CUSTOMER_ORDERS_RETIRED_MESSAGE =
  "Legacy orders are retired. Use Add Comics (photo, manual, acquisitions) for new inventory. Existing orders remain viewable.";
