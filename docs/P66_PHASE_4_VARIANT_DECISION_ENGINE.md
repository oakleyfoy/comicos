# P66 Phase 4 — Variant Decision Engine

Ranks covers per issue, emits buy/skip plans and quantity allocation, and summarizes premium rationale (e.g. foil vs standard FMV delta from stub pricing).

Endpoints: `GET/POST /api/v1/variant-decision/latest|build`, `POST .../platform/build`, `GET .../integration/latest`, `GET .../platform/certification`
